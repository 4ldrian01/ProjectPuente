# ==============================================================
# ProjectPuente - Backend and local runtime dependencies
# ==============================================================
# Install: pip install -r backend/requirements.txt
# ==============================================================

# Django API runtime
django>=5.0,<7.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3,<5.0
python-dotenv>=1.0,<2.0

# Optional relational DB driver (SQLite is default in this project)
psycopg2-binary>=2.9,<3.0

# NLLB-200 + LoRA runtime stack
torch>=2.2.0
transformers>=4.44.0
huggingface_hub>=0.24.0
sentencepiece>=0.2.0
accelerate>=0.30.0
peft>=0.11.0
bitsandbytes>=0.43.0
protobuf>=4.25.0

# Optional speech synthesis (requires outbound internet to Microsoft TTS)
edge-tts>=7.2.0,<8.0

# Telemetry
psutil>=5.9.8
GPUtil>=1.4.0

# Evaluation + data/pipeline utilities used by bundled scripts and preflight
sacrebleu>=2.4.0
pandas>=2.2.0
pdfplumber>=0.11.0
ijson>=3.3.0
